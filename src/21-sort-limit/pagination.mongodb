use("platzi_store")

/*db.categories
  .find()
  .skip(0)
  .limit(3)

 db.categories
  .find()
  .skip(3)
  .limit(3)

db.categories
  .find()
  .skip(6)
  .limit(3)*/

db.categories
  .find()
  .skip(9)
  .limit(3)